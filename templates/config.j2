---
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml
{% if promtail_client_configs | default(false) %}

clients:
  {{ promtail_client_configs | to_nice_yaml(indent=2) | indent(width=2, first=false) | trim }}
{% endif %}
{% if promtail_scrape_configs | default(false) %}

scrape_configs:
  {{ promtail_scrape_configs | to_nice_yaml(indent=2) | indent(width=2, first=false) | trim }}
{% endif %}

...
